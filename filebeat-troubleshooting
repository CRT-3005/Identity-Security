# 🛠 Filebeat not shipping Wazuh alerts

**Symptoms I hit**
- `systemctl status filebeat` exits with code **2** (YAML/config error) or runs but **Discover is empty**.
- Logs don’t show a stable connection to `https://127.0.0.1:9200`.
- No “Harvester started for file: /var/ossec/logs/alerts/alerts.json”.

## 1) Quick health checks
```bash
systemctl status filebeat --no-pager
journalctl -u filebeat -e --no-pager
sudo filebeat test config -e
sudo filebeat test output -e
sudo tail -n 50 /var/ossec/logs/alerts/alerts.json

